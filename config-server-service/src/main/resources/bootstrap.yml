server:
  port: 8888
spring:
  application:
    name: config-server-service
  cloud:
   config:
     server:
       git:
         force-pull: true
         uri: https://github.com/JaspalKaurBatra/config-server-repo.git